collection = { 1,2,3,4,4} # you can see that the value 4 is stored twice , but in the terminal its only counting one 4 , and even in the length it is showing total length as 4
print(collection)
print(type(collection))
print(len(collection))
